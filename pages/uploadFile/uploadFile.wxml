<!--logs.wxml-->
<view class="container uploader-box">
	<van-uploader file-list="{{ fileList }}" bind:after-read="afterRead" />
	<canvas canvas-id='attendCanvasId' id='myCanvas' style="width: {{cw}}px; height: {{ch}}px;"></canvas>

</view>